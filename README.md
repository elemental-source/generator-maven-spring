# generator-maven-spring
[Yeoman generator](http://yeoman.io) for creating Maven-based projects integrated to the Spring Framework

## Install
To install run:
- Link: `npm link`
- Install: `npm install`
- Run: `yo maven-spring:app`
